export const mockTranscripts = {
  baseline:
    "Indexing works by creating a separate data structure — typically a B-tree or hash map — that stores the values of one or more columns alongside pointers to the actual rows in the table. When a query hits an indexed column, the database engine traverses the index instead of performing a full table scan. A B-tree index, for instance, keeps values in sorted order, which means range queries like 'WHERE age BETWEEN 25 AND 35' can be resolved by finding the start of the range in O(log n) time and then doing a sequential read from that point forward. The tradeoff is that every INSERT, UPDATE, or DELETE now has to maintain the index structure as well, so write-heavy workloads can actually be hurt by over-indexing. In practice, the index is most valuable on columns that appear frequently in WHERE clauses, JOIN conditions, or ORDER BY expressions. Composite indexes — indexes spanning multiple columns — require understanding column ordering because the leftmost prefix rule determines which queries the index can serve. A composite index on (last_name, first_name) will help a query filtering on last_name alone, but not one filtering only on first_name.",
  pressure:
    "Okay so, um, indexing can definitely hurt performance because every time you insert or update a row the database also has to, uh, update the index structure — so writes get noticeably slower. And also, um, if you index a column that doesn't get queried much you're just wasting disk space and, uh, buffer pool memory basically for no real gain. And there's also the thing where, like, the query planner can pick the wrong index? Like it might use a less selective index instead of the better one, and um... yeah that causes slowdowns. And foreign key columns should usually be indexed too, because... wait, I think I said that already. Yeah, because joins would be slow otherwise. And um, cardinality matters — like you wouldn't index a boolean column because there are only two distinct values so the full scan is basically just as fast anyway.",
  retry:
    "Indexes speed up reads at the cost of slower writes, because every data modification must also update the index. Definition: an index is a purpose-built data structure — usually a B-tree — that stores column values alongside row pointers, letting the engine skip full table scans. Reason: that bookkeeping costs time on every write and memory in the buffer pool, so over-indexing high-throughput tables directly degrades ingestion throughput. Example: a distributed logging pipeline that indexes every column for ad-hoc analytics can see a 4× write slowdown under load compared to a targeted index strategy. The practical principle is to index selectively — prioritise columns in WHERE clauses, JOIN conditions, and high-cardinality ORDER BY expressions, and regularly drop unused indexes as the query workload evolves.",
} as const;

export type TranscriptStage = keyof typeof mockTranscripts;
